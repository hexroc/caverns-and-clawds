/**
 * Class Features - Unified Export
 * All class-specific features for Fighter, Rogue, Cleric, Wizard, Warlock, Paladin, Bard
 */

const fighter = require('./fighter');
const rogue = require('./rogue');
const cleric = require('./cleric');
const wizard = require('./wizard');
const warlock = require('./warlock');
const paladin = require('./paladin');
const bard = require('./bard');

module.exports = {
  fighter,
  rogue,
  cleric,
  wizard,
  warlock,
  paladin,
  bard
};
