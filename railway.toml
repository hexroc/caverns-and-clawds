[build]
builder = "NIXPACKS"

[deploy]
healthcheckPath = "/api/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

# Add volume mount for SQLite database persistence
[[deploy.volumes]]
mountPath = "/app/db"
name = "caverns-db"